# Logo files for PDF generation

Add these files here for full logo/crest rendering in PDFs:

- `logo-crest.png` – Crest/shield logo (or use `logo.png` / `logo.jpeg` / `logo.jpg`)
- `logo-text.png` – Text logo image

If these files are missing, a minimal text fallback will be used.
